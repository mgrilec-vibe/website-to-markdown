---
description: Implement tasks from an OpenSpec change.
input:
  - change: Optional change name
argument-hint: "[change name]"
---

Implement the OpenSpec change ${input:change}. Use the `openspec-apply-change` skill and follow its workflow. If no change is supplied, follow the skill's selection procedure.
