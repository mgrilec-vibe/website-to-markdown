---
description: Archive a completed OpenSpec change.
input:
  - change: Optional change name
argument-hint: "[change name]"
---

Archive the OpenSpec change ${input:change}. Use the `openspec-archive-change` skill and follow its workflow. If no change is supplied, follow the skill's selection procedure.
