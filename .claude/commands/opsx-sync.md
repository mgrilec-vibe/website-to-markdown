---
argument-hint: '[change name]'
arguments:
- change
description: Sync an OpenSpec change's delta specs to main specs.
---

Sync the delta specs for OpenSpec change $change to the main specs. Use the `openspec-sync-specs` skill and follow its workflow. If no change is supplied, follow the skill's selection procedure.