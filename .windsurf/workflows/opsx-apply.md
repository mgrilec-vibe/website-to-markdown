---
argument-hint: '[change name]'
arguments:
- change
description: Implement tasks from an OpenSpec change.
---

Implement the OpenSpec change $change. Use the `openspec-apply-change` skill and follow its workflow. If no change is supplied, follow the skill's selection procedure.