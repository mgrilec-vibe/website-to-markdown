import { defineConfig } from 'wxt';

export default defineConfig({
  manifest: {
    name: 'Chrome Local AI Assessment Kit',
    description: 'Runs local-only Chrome Summarizer API assessment fixtures.',
    version: '0.1.0',
    permissions: [],
    host_permissions: [],
  },
});
